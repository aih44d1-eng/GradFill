# Your signing key. Never commit this.
gradfill.pem

# Backups exported from the setup page — these contain your personal details.
gradfill-details*.json

*.crx
*.zip
.DS_Store

# Node dependencies and local env files — never commit real secrets
# (DATABASE_URL, ANTHROPIC_API_KEY, STRIPE_*, COINBASE_*, etc).
node_modules/
.env
.env.*
!.env.example

# Local git worktrees used for isolated feature work.
.worktrees/
